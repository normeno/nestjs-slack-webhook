{
  "name": "nestjs-slack-webhook",
  "version": "0.0.1",
  "description": "NestJS Slack Webhook Integration",
  "main": "dist/index",
  "type": "dist/index",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "rimraf dist && tsc",
    "prepublish": "npm run build"
  },
  "author": "Nicolás Ormeño",
  "license": "MIT",
  "dependencies": {
    "@nestjs/common": "^8.0.2",
    "@slack/webhook": "^6.0.0",
    "rxjs": "^7.2.0"
  },
  "devDependencies": {
    "@types/node": "^16.3.2",
    "rimraf": "^3.0.2",
    "typescript": "^4.3.5"
  }
}
